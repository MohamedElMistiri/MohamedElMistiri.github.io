<div style="clear: both;">
  <img src="{{ include.image }}" alt="{{ include.alt }}" style="float: left; margin-right: 20px; margin-bottom: 10px; width: {{ include.width | default: '300px' }};">
  {{ include.content | markdownify }}
</div>
<style>
  .float-content ul {
    padding-left: 40px !important;
    /* No other style properties to preserve theme styling */
  }
  /* Preserve bullet color and appearance */
  .float-content ul li {
    /* Reset any unwanted styling to match theme defaults */
    color: inherit;
    list-style-type: disc;
  }
</style>